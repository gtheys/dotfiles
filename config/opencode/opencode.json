{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "postgres": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@henkey/postgres-mcp-server"
      ],
      "enabled": true,
      "environment": {
        "POSTGRES_CONNECTION_STRING": "postgres://postgres:localpassword@localhost:5432/salaryhero?sslmode=disable"
      }
    }
  }
}
