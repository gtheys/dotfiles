#!/bin/sh
#
# update
#
# `update` handles installation, updates, things like that. Run it periodically
# to make sure you're on the latest and greatest.

# Set OS X defaults
$CZSH/osx/set-defaults.sh

# Upgrade homebrew and bundles
brew update & brew bundle